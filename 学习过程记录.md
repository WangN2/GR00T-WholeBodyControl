# GR00T-WholeBodyControl 学习过程记录

> 记录每日学习进展、对话要点、问题与解答。
> 学习计划见：[STUDY_PLAN.md](./STUDY_PLAN.md)

---

## 📊 总体学习进度

```
[████░░░░░░░░░░░░░░░░] 阶段 1/6  ·  地基建设中  ·  约 15%

阶段 ① 地基建设        [████░░░░░░] 40%  ⬅️ 当前
阶段 ② Isaac Lab 环境  [░░░░░░░░░░]  0%
阶段 ③ SONIC 核心架构  [░░░░░░░░░░]  0%
阶段 ④ 数据流与运动跟踪 [░░░░░░░░░░]  0%
阶段 ⑤ Decoupled WBC  [░░░░░░░░░░]  0%
阶段 ⑥ 综合实战        [░░░░░░░░░░]  0%
```

---

## 2026-05-09（Day 1）| 阶段 ① · 地基建设

### 今日主题
实操入门 + 概念扫盲。用户此前没有接触过 Isaac Lab 和机器人 RL，从零开始跑通第一次训练。

### 对话要点

#### 1. Isaac Lab 基础操作
- **问题**：Isaac Lab 视野不见了怎么恢复？
- **解答**：快捷键 `F`（聚焦选中物体）、`A`（聚焦所有物体）、`Ctrl+F`（相机聚焦）。代码中可用 `env.viewport_camera_controller.update_view_to_asset_root("robot")` 重置。

#### 2. 训练环境检查
- 发现系统中有 **roc.wang 的 LeRobot pi05 LoRA 微调**在跑（双卡 RTX 4090）
- 发现用户自己有一个长期运行的 **MuJoCo 仿真循环**（`run_sim_loop.py`，已跑 7 天）
- 帮用户停掉了 MuJoCo 仿真循环（PID 490620）

#### 3. 启动第一次 Isaac Lab 训练
- **命令**：`./isaaclab.sh -p scripts/reinforcement_learning/rsl_rl/train.py --task Isaac-Velocity-Rough-G1-v0 --headless --num_envs 4096 --seed 0`
- **实际进程**：`num_envs=2048`（用户记忆有误）
- **训练时长**：约 3 小时 40 分钟（15:40 → 19:20）
- **完成状态**：3000/3000 iterations 全部完成
- **最终模型**：`model_2999.pt`（7.5 MB）
- **最终指标**：
  - Mean Reward：15.09（初始 -0.65）
  - Episode Length：952.1 步（初始 12.8 步）
  - 91.87% episode 因 time_out 终止（极少摔倒）
  - 课程地形等级：5.88 / 8

#### 4. Python 基础概念补盲
- **装饰器（Decorator）**：本质是 `func = decorator(func)` 的语法糖，用于增强函数功能
- **语法糖（Syntactic Sugar）**：让代码写起来更简洁，不改变底层逻辑
- **`@hydra_task_config`**：Isaac Lab 用来桥接注册表配置和 Hydra 命令行覆盖的装饰器
- **不用装饰器的等价写法**：手动调用 `register_task_to_hydra()` + `@hydra.main()` + 配置转换

#### 5. 强化学习概念
- **DAgger（Dataset Aggregation）**：模仿学习的迭代式数据收集算法。让策略自己跑，在跑偏时让专家标注纠正，解决分布偏移问题。
- **训练原理**：这次训练是 **纯强化学习（PPO + RSL-RL）**，没有数据集，所有数据由 Isaac Sim 物理引擎实时生成（2048 个环境并行）。
- **模型架构**：
  - 算法：PPO（Proximal Policy Optimization）
  - 网络：Actor-Critic MLP，隐藏层 `[512, 256, 128]`，激活函数 ELU
  - 动作分布：对角协方差高斯（Diagonal Gaussian），σ 是可学习参数
  - 总参数量：约 30-50 万

#### 6. 训练指标详解
对 iteration 2999 的最终日志逐项讲解：
- **效率指标**：Total steps 1.47 亿，FPS 34,754，单轮 iteration 1.41 秒
- **Loss 指标**：Value Loss 0.0144（很小），Surrogate Loss -0.0072（收敛），Entropy 44.88（保留探索）
- **Reward 分解**：
  - 正向：`track_lin_vel_xy_exp`（0.76）、`track_ang_vel_z_exp`（0.89）、`feet_air_time`（0.0085）
  - 负向：`action_rate_l2`（-0.54，最大负项，动作略抖）、`joint_deviation_arms`（-0.12）
- **终止原因**：91.87% time_out（极好），8.13% base_contact（极少摔倒）

#### 7. 训练产物位置
```
~/Projects/IsaacLab/logs/rsl_rl/g1_rough/2026-05-09_15-40-23/
├── model_2999.pt          # 最终模型
├── model_0.pt ~ model_2950.pt  # 每 50 iter 一个 checkpoint（共 60 个）
├── events.out.tfevents... # TensorBoard 日志
├── params/agent.yaml      # PPO 超参数
├── params/env.yaml        # 环境配置（奖励权重等）
└── git/IsaacLab.diff      # 代码改动快照
```

### 已完成检查点 ✅

- [x] 第一次跑通 Isaac Lab 训练（RSL-RL PPO）
- [x] 理解 Reward、Episode Length、FPS 等基本训练指标
- [x] 理解 PPO 的 on-policy 特性（无数据集，实时生成数据）
- [x] 理解 Actor-Critic MLP 的基本结构
- [x] 理解高斯策略（μ + σ * ε）的采样机制
- [x] 理解 ELU 比 ReLU 更适合机器人控制的原因
- [x] 找到训练产物目录

### 遗留问题 ❓

- [ ] 尚未阅读 PPO 原始论文（Schulman 2017）
- [ ] 尚未阅读 GAE 论文（Schulman 2016）
- [ ] 尚未理解 Isaac Lab 的 6 大 Manager 体系
- [ ] 尚未理解 Hydra 配置组合的完整机制
- [ ] 尚未理解 `gear_sonic` 自研 TRL 与 RSL-RL 的区别

### 下一步计划

1. 阅读 PPO + GAE 论文（阶段 ① 核心任务）
2. 用 Stable-Baselines3 跑一个独立的 PPO 小实验（Humanoid-v4）
3. 继续阶段 ① 的其他前置知识（Pinocchio FK、SMPL 基础）

---

## 2026-05-11（Day 2）| 阶段 ① · 地基建设（续）

### 今日主题
训练结果验证尝试 + GPU 资源管理 + 系统性学习计划制定。

### 对话要点

#### 1. 多次尝试运行 play.py 验证模型
- **目标**：用训练好的 `model_2999.pt` 做可视化验证
- **遇到的问题**：GPU 显存不足（Out of GPU Memory）
- **根本原因**：
  - roc.wang 的 `genie_rl` 推理服务占用双卡各 ~14.5GB
  - Docker 里的 `genie_sim` 评测任务占用双卡各 ~6.5GB
  - 两张 RTX 4090 都只剩 ~2-3GB 空闲
  - Isaac Sim Kit 的 Vulkan 初始化需要在 GPU 0 上分配 ~4-6GB，无论 `--device cuda:1` 如何指定
- **尝试的命令**：
  ```bash
  # 第 1 次：无 headless，num_envs=32 → 爆显存
  ./isaaclab.sh -p .../play.py --task Isaac-Velocity-Rough-G1-v0 --num_envs 32

  # 第 2 次：加 --headless，num_envs=4 → 仍爆（genie_sim 还在跑）
  ./isaaclab.sh -p .../play.py --task ... --headless --num_envs 4 --device cuda:1

  # 第 3 次：加 --headless --video --video_length 200 → 仍爆（--video 触发 Vulkan 渲染）
  ./isaaclab.sh -p .../play.py --task ... --num_envs 6 --device cuda:1 --headless --video --video_length 200
  ```
- **关键认知**：`--device cuda:1` 只控制 CUDA 计算层，**Omniverse Kit 的 Vulkan 层强制绑定 GPU 0**（主显示卡）。
- **当前状态**：play.py 仍未能成功运行，需等 GPU 资源释放。

#### 2. 系统性学习计划制定
- 完成了对项目的全面探索（3 个 explore 代理并行）
- 梳理了三大子系统：`decoupled_wbc`、`gear_sonic`、`gear_sonic_deploy`
- 梳理了关键技术栈、论文、数据流、模型架构
- 输出完整学习计划文档：[STUDY_PLAN.md](./STUDY_PLAN.md)
- **核心发现**：
  - 用户之前跑的 RSL-RL 只是 Isaac Lab 官方示例，**不是项目核心训练系统**
  - 项目真正的训练框架是 `gear_sonic/trl/`（基于 HuggingFace TRL 自研的 PPO + Auxiliary Losses）
  - 核心模型是 **Universal Token Module**（多编码器 + FSQ 量化 + 多解码器）

### 已完成检查点 ✅

- [x] 理解 GPU 显存分配机制（CUDA vs Vulkan 分层）
- [x] 理解 Isaac Sim Kit 的启动显存需求（headless ~5-6GB，GUI ~8-12GB）
- [x] 了解项目三大子系统的架构关系
- [x] 获得完整 12 周学习计划
- [x] 了解 Universal Token Module 的宏观架构

### 遗留问题 ❓

- [ ] play.py 尚未成功运行（GPU 资源问题）
- [ ] 尚未实际接触 `gear_sonic/trl/` 代码
- [ ] 尚未阅读 SONIC 论文
- [ ] 尚未理解 FSQ 量化的数学原理

### 下一步计划

1. 等 GPU 资源空闲后，成功运行一次 play.py（阶段 ② 前置体验）
2. 按学习计划进入阶段 ①：精读 PPO + GAE 论文
3. 阅读 `docs/算法解析/逆运动学(IK)初学者指南.md`

---

## 学习统计

| 指标 | 数值 |
|------|------|
| 累计学习天数 | 2 天 |
| 完成阶段 | 阶段 ①（进行中） |
| 已读论文 | 0 篇 |
| 已跑实验 | 1 次（RSL-RL G1 Rough 训练） |
| 已写代码 | 0 行 |
| 已解决问题 | 8 个（视野恢复、训练检查、指标讲解、装饰器、DAgger、GPU 排查等） |
| 待解决问题 | 4 个 |

---

---

## 2026-05-11（Day 3）| 阶段 ① · 地基建设（论文精读）

### 今日主题
PPO + GAE 论文精读 + 项目真实代码对照 + SB3 动手实验。

### 对话要点

#### 1. 项目全景扫描
- 完整探索了 `docs/GR00T-WholeBodyControl 项目文件结构详解.md`
- 完整探索了 `docs/GR00T-SONIC 完整技术文档.md`
- 完整探索了 `docs/GR00T-WholeBodyControl 安装与复现手册.md`
- 扫描了 `docs/算法解析/逆运动学(IK)初学者指南.md` 和 `人体到G1重定向的数学原理.md`
- 建立了三大子系统的完整认知地图：
  - `gear_sonic/` — SONIC 统一策略（Python，Isaac Lab 训练）
  - `decoupled_wbc/` — 解耦 WBC（Python，RL 下肢 + IK 上肢）
  - `gear_sonic_deploy/` — C++ 实机部署（TensorRT/ONNX，4 线程实时）

#### 2. PPO 论文核心（Schulman 2017）
- **Clipped Surrogate Objective**：
  $$L^{CLIP}(\theta) = \mathbb{E}_t \left[ \min\left( r_t(\theta) A_t,\; \text{clip}(r_t(\theta), 1-\epsilon, 1+\epsilon) A_t \right) \right]$$
- **概率比** $r_t(\theta) = \pi_\theta / \pi_{\theta_{old}}$ 衡量策略变化幅度
- **Clip 操作**将 $r_t$ 限制在 $[1-\epsilon, 1+\epsilon]$，天然实现 Trust Region
- **min 操作**确保优化的是更保守的目标，防止策略暴走
- 项目代码对应：`gear_sonic/trl/trainer/ppo_trainer.py:1397-1408`

#### 3. GAE 论文核心（Schulman 2016）
- **TD residual**：$\delta_t = r_t + \gamma V(s_{t+1}) - V(s_t)$
- **GAE 递归公式**：$\hat{A}_t = \delta_t + \gamma\lambda \hat{A}_{t+1}$
- **两个极端**：$\lambda=0$ 时退化为 TD(0)；$\lambda=1$ 时退化为 Monte Carlo
- **实际配置**：项目使用 $\gamma=0.99, \lambda=0.95$
- 项目代码对应：`gear_sonic/trl/trainer/ppo_trainer.py:2112-2120`
- 优势归一化：`mean=0, std=1`，`1e-8` 防止除零

#### 4. 辅助损失（Auxiliary Loss）
- 读取了 `gear_sonic/trl/trainer/ppo_trainer_aux_loss.py`
- 总 loss = `ppo_loss + aux_loss_scale * sum(coef_i * aux_loss_i)`
- 这是 SONIC / Universal Token Module 训练的关键扩展

#### 5. SB3 PPO 动手实验
- 环境：`Pendulum-v1`（连续动作空间）
- 训练 20K steps，约 1 分钟
- 观察了 5 个关键指标：`policy_loss`, `value_loss`, `entropy`, `approx_kl`, `clip_fraction`
- 理解了每个指标的健康范围和物理含义

### 已完成检查点 ✅

- [x] 精读 PPO 原始论文核心公式（Clipped Surrogate Objective）
- [x] 精读 GAE 论文核心公式（递归优势估计）
- [x] 对照项目真实代码理解 PPO + GAE 的实现细节
- [x] 跑通 Stable-Baselines3 PPO 独立实验
- [x] 理解 5 个关键训练指标的含义和健康范围
- [x] 建立项目三大子系统的完整架构认知
- [x] 理解 Universal Token Module 的宏观数据流

### 遗留问题 ❓

- [ ] play.py 尚未成功运行（GPU 资源问题）
- [ ] 尚未深入阅读 `gear_sonic/trl/modules/universal_token_modules.py`
- [ ] 尚未理解 FSQ 量化的数学原理
- [ ] 尚未阅读 SONIC 论文（arXiv:2511.07820）
- [ ] 尚未跑 Pinocchio FK 实验

### 下一步计划

1. **补完阶段 ① 剩余任务**：
   - 阅读 `docs/算法解析/逆运动学(IK)初学者指南.md`（QP-IK 数学推导）
   - 用 Pinocchio 算一次 G1 前向运动学
   - 理解 SMPL beta/pose 参数
2. **进入阶段 ②**：Isaac Lab 6 大 Manager 体系
3. **进入阶段 ③**：精读 Universal Token Module 代码

---

## 学习统计

| 指标 | 数值 |
|------|------|
| 累计学习天数 | 3 天 |
| 完成阶段 | 阶段 ①（进行中，~65%） |
| 已读论文 | 2 篇（PPO 2017, GAE 2016） |
| 已跑实验 | 2 次（RSL-RL G1 Rough + SB3 Pendulum） |
| 已写代码 | 1 个脚本（SB3 PPO 实验） |
| 已解决问题 | 12 个 |
| 待解决问题 | 4 个 |

---

*本文档由 Kimi 每日自动更新。如有遗漏或需要补充，请随时提醒。*
